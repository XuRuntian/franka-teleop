"""Standalone Franka teleoperation utilities.

Import concrete modules directly, for example:
`from franka_teleop.client import FrankaHttpClient`.
This keeps configuration/state helpers importable without SpaceMouse hardware
dependencies being installed in the active Python environment.
"""
